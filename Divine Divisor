number=int(input())

divisor=list()

for i in range(1,int(number**0.5)+1):
    if number%i==0:
        print(i,end=" ")
        if (number/i!=i):
            divisor.append(int(number/i))
            
divisor.sort()
print(*divisor,end=" ")
